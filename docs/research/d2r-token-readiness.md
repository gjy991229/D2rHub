# D2R Token 启动完成判定研究

## 当前测试结论（2026-08-26）

`fix/token-readiness` 采用激进的单一判据：只有目标 `D2R.exe` PID 成功读取注册表值 `WEB_TOKEN`，才能启动下一个 Token 账号。Token 流程不再读取 TCP 表、不再等待游戏窗口，也不再依赖 `WEB_TOKEN` 内容发生回写。

监听对象是 Windows ETW provider `Microsoft-Windows-Kernel-Registry`（GUID `70eb4f03-c1de-4f73-a051-33d13d5413bd`）的 `QueryValue` 事件（事件 ID 7）。事件必须同时满足：

- 事件 PID 等于刚启动的目标 D2R PID；
- `ValueName` 不区分大小写等于 `WEB_TOKEN`；
- `Status` 等于 0，表示查询成功。

这不是在读取或导出 Token 内容。监听器只保留满足条件的 PID、匹配事件数和解析错误数。

## 为什么取消 TCP

TCP `ESTABLISHED` 只能证明进程存在可传输数据的连接，不能证明认证 Token 已被消费。认证、遥测、CDN、代理和大厅连接都可能提前满足；加速器还可能改变端口或连接所属 PID。因此固定 1119、443 或“任意已建立连接”都不能作为 Token 可以被下一账号覆盖的可靠信号。

Battle.net 客户端启动分支仍可保留自己的网络就绪逻辑；本次取消仅针对 Token 直接启动流程。

## 状态机

1. 将账号 Token 写入共享 `WEB_TOKEN`。
2. 在创建 D2R 进程前启动 ETW 监听，避免快速读取发生在 PID 发现之前而漏报。
3. 启动 D2R 并发现新 PID。监听器会保留此前捕获的成功读取 PID，因此此时即可反查。
4. 每 50 ms 检查目标 PID 是否已成功读取 `WEB_TOKEN`；命中后立即允许启动下一个账号。
5. 60 秒仍未命中则失败，并记录匹配事件数、成功读取过的 PID 和解析错误数。
6. ETW 会话启动失败时直接报错，不退回 TCP、窗口或注册表回写判据。

跳过开场动画和清除互斥句柄仍是独立后台任务，不参与 Token 消费判定。

## 已知风险与验证点

- 应用清单要求管理员权限；非提升权限的开发终端启动 Kernel Registry provider 会返回“拒绝访问”。正式程序的提升权限运行方式需要实机确认。
- 当前假设 `WEB_TOKEN` 由 `D2R.exe` 自身读取。如果实测事件显示由中间辅助进程读取，严格 PID 过滤会超时；超时诊断中的“成功读取 PID”可用于确认这一点。
- `WEB_TOKEN` 名称本身很有辨识度，但当前版本不解析 `KeyObject` 对应的完整注册表路径。这是本测试分支刻意接受的简化。
- ETW 事件消费尚需用国际服实际登录验证。受控集成测试已保留，但因需要提升权限默认忽略。
