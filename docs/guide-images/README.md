# 上手图示素材

2026-09-12 从本地 `visual-audit.html` 截取当前真实 React 组件；账号、Mod 名称和运行状态来自已有浏览器示例数据，不代表用户电脑的状态。没有启动游戏、执行加工、扫描键位或修改桌面配置。

原始 PNG 为 1280×720。`guide-figures.js` 使用 SVG viewBox 显示局部，框选坐标以原图为基准；原始图片不重绘。页面上的编号可点击强调，支持查看原图。组件布局变化后需要重新截图并更新坐标。

- recognition.png：识别与统计，待准备 Mod 的示例状态。
- mod-processing.png：识别入口前往加工，填写示例名 HubMain，未执行加工。
- room-participants.png：跟房页面顶部的主号、小号与成品选择。
- room-method.png：跟房页面下滚后的后台按键与手动跟随设置。
- example.html：复用同一标注组件的 Mod 加工示例，供独立预览。

所有素材随现有 docs 资源目录一起打包，无需联网获取。

## 第二组素材

- account-init.png：初始化新账号的昵称步骤，不登录、不粘贴真实凭据。
- accounts.png：账号配置、当前 Mod 和未初始化状态，仅作配置页说明。
- paths.png：国服 / 国际服与隔离浏览器路径；均为示例路径。
- modules.png：识别与统计、自动跟房卡片，示例已添加状态。
- room-rules.png：房名规则、密码和后台聊天键扫描入口，没有执行扫描。
- room-auto.png：仅在模拟页面切到自动跟随，展示延时输入，没有执行跟房。

本步图示有多张时，通过上方编号按钮切换；只有当前图示挂载，避免重复裁切标识。未拍摄的主界面启动、游戏内入房和真实统计结果仍使用文字完成标志，不以模拟状态代替结果。
